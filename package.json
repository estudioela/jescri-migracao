{
  "name": "projeto-tear-v2",
  "version": "0.0.0",
  "private": true,
  "description": "TEAR V2 — Google Apps Script / Sheets. Fundação técnica (Sprint 0).",
  "type": "commonjs",
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "check": "npm run lint && npm test",
    "preview": "node scripts/preview-server.mjs"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "<rootDir>/test/**/*.test.js"
    ]
  }
}
