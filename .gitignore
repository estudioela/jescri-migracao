.DS_Store
*.log
node_modules/
coverage/
.claude/worktrees/
archive/
_backup_full_*/
_backup_cleanup_*/
_backup_stitch_consolidacao_*/
_archive_legacy_stitch/
# Config real do clasp (contém scriptId; operador provisiona a partir de .clasp.json.example)
.clasp.json
.env

# Backups locais do clasp
mae/.clasp.producao.backup.json

# Dados exportados da planilha de produção — contêm PII (PIX, CNPJ, e-mail, endereço).
# Nunca versionar: este repositório é público.
tools/*.csv
dados_limpos.json

# Seed de migração: nomes reais de influenciadoras + chave primária corrompida (ON/OFF).
# Não versionar até ser regenerado pelo ExportadorDeDados.
tear/DataSeed.js
tear/Importador.js

# Artefato temporário gerado por `nlm source list` (manutenção do NotebookLM)
sources.json

# Configuração local do Claude Code (permissões por máquina)
.claude/settings.local.json
